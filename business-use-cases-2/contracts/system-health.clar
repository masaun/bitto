(define-map system_health_registry uint {creator: principal, data: (string-ascii 256), timestamp: uint})
(define-data-var system_health_nonce uint u0)
(define-read-only (get-entry (id uint)) (map-get? system_health_registry id))
(define-public (register (data (string-ascii 256)))
  (let ((id (+ (var-get system_health_nonce) u1)))
    (map-set system_health_registry id {creator: tx-sender, data: data, timestamp: stacks-block-height})
    (var-set system_health_nonce id)
    (ok id)))
(define-public (update-entry (id uint) (data (string-ascii 256)))
  (let ((entry (unwrap! (map-get? system_health_registry id) (err u1))))
    (asserts! (is-eq tx-sender (get creator entry)) (err u2))
    (map-set system_health_registry id (merge entry {data: data}))
    (ok true)))
