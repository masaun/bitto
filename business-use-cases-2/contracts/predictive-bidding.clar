(define-map predictive_bidding_registry uint {creator: principal, data: (string-ascii 256), timestamp: uint})
(define-data-var predictive_bidding_nonce uint u0)
(define-read-only (get-entry (id uint)) (map-get? predictive_bidding_registry id))
(define-public (register (data (string-ascii 256)))
  (let ((id (+ (var-get predictive_bidding_nonce) u1)))
    (map-set predictive_bidding_registry id {creator: tx-sender, data: data, timestamp: stacks-block-height})
    (var-set predictive_bidding_nonce id)
    (ok id)))
(define-public (update-entry (id uint) (data (string-ascii 256)))
  (let ((entry (unwrap! (map-get? predictive_bidding_registry id) (err u1))))
    (asserts! (is-eq tx-sender (get creator entry)) (err u2))
    (map-set predictive_bidding_registry id (merge entry {data: data}))
    (ok true)))
