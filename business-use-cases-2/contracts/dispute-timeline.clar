(define-map dispute_timeline_registry uint {creator: principal, data: (string-ascii 256), timestamp: uint})
(define-data-var dispute_timeline_nonce uint u0)
(define-read-only (get-entry (id uint)) (map-get? dispute_timeline_registry id))
(define-public (register (data (string-ascii 256)))
  (let ((id (+ (var-get dispute_timeline_nonce) u1)))
    (map-set dispute_timeline_registry id {creator: tx-sender, data: data, timestamp: stacks-block-height})
    (var-set dispute_timeline_nonce id)
    (ok id)))
(define-public (update-entry (id uint) (data (string-ascii 256)))
  (let ((entry (unwrap! (map-get? dispute_timeline_registry id) (err u1))))
    (asserts! (is-eq tx-sender (get creator entry)) (err u2))
    (map-set dispute_timeline_registry id (merge entry {data: data}))
    (ok true)))
