(define-map performance_bond_registry uint {creator: principal, data: (string-ascii 256), timestamp: uint})
(define-data-var performance_bond_nonce uint u0)
(define-read-only (get-entry (id uint)) (map-get? performance_bond_registry id))
(define-public (register (data (string-ascii 256)))
  (let ((id (+ (var-get performance_bond_nonce) u1)))
    (map-set performance_bond_registry id {creator: tx-sender, data: data, timestamp: stacks-block-height})
    (var-set performance_bond_nonce id)
    (ok id)))
(define-public (update-entry (id uint) (data (string-ascii 256)))
  (let ((entry (unwrap! (map-get? performance_bond_registry id) (err u1))))
    (asserts! (is-eq tx-sender (get creator entry)) (err u2))
    (map-set performance_bond_registry id (merge entry {data: data}))
    (ok true)))
