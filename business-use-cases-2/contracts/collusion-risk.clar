(define-map collusion_risk_registry uint {creator: principal, data: (string-ascii 256), timestamp: uint})
(define-data-var collusion_risk_nonce uint u0)
(define-read-only (get-entry (id uint)) (map-get? collusion_risk_registry id))
(define-public (register (data (string-ascii 256)))
  (let ((id (+ (var-get collusion_risk_nonce) u1)))
    (map-set collusion_risk_registry id {creator: tx-sender, data: data, timestamp: stacks-block-height})
    (var-set collusion_risk_nonce id)
    (ok id)))
(define-public (update-entry (id uint) (data (string-ascii 256)))
  (let ((entry (unwrap! (map-get? collusion_risk_registry id) (err u1))))
    (asserts! (is-eq tx-sender (get creator entry)) (err u2))
    (map-set collusion_risk_registry id (merge entry {data: data}))
    (ok true)))
